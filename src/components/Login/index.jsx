import {useState} from 'react'
import {Navigate,useLocation,useNavigate} from 'react-router-dom'
const LOGIN_API='https://apis.ccbp.in/login'
const LOGO='./assets/nxt-assess-logo.svg'

export default function Login(){const[user,setUser]=useState('');const[password,setPassword]=useState('');const[show,setShow]=useState(false);const[error,setError]=useState('');const[loading,setLoading]=useState(false);const navigate=useNavigate();const location=useLocation();if(localStorage.getItem('jwt_token'))return <Navigate to="/" replace/>;const submit=async e=>{e.preventDefault();setError('');setLoading(true);try{const response=await fetch(LOGIN_API,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:user,password})});const data=await response.json();if(!response.ok)throw new Error(data.error_msg||'Unable to sign in');localStorage.setItem('jwt_token',data.jwt_token);navigate(location.state?.from?.pathname||'/',{replace:true})}catch(err){setError(err.message||'Unable to sign in. Please try again.')}finally{setLoading(false)}};return <main className="login-page"><section className="login-panel"><div className="orb orb-one"/><div className="orb orb-two"/><div className="login-copy"><div className="illustration-icon">✦</div><h1>Feel prepared.<br/>Do your best.</h1><p>A simple, calm place to put your learning into practice.</p><div className="copy-note">10 questions · 10 minutes · instant result</div></div></section><section className="login-form-wrap"><form className="login-form" onSubmit={submit}><img className="login-logo" src={LOGO} alt="login website logo"/><h2>Welcome back</h2><p>Sign in to begin your assessment.</p><label htmlFor="username">USERNAME</label><input id="username" value={user} onChange={e=>setUser(e.target.value)} placeholder="Enter your username" required/><label htmlFor="password">PASSWORD</label><input id="password" value={password} type={show?'text':'password'} onChange={e=>setPassword(e.target.value)} placeholder="Enter your password" required/><label className="show-password"><input type="checkbox" checked={show} onChange={e=>setShow(e.target.checked)}/> Show password</label>{error&&<p className="form-error">{error}</p>}<button className="primary" disabled={loading}>{loading?'Signing in...':'Login'}</button></form></section></main>}
